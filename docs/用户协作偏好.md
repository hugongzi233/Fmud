---
name: user_preferences
description: User collaboration preferences - short agents, root dir docs
type: user
---

**协作偏好：**

1. Agent 使用短上下文：每次 agent 任务只读关键文件片段，全文件读取不超过 2 个，宁可分多次跑也不开超长上下文
2. 文档写根目录：所有分析/计划文档写在 h:\fmud\ 根目录，不要写在 .claude/ 子目录
3. 不要用 Read tool 的 pages 参数（会报错）
4. 模型配置：/model MiniMax-M2.7（项目根目录下的 模型.txt 文件）
